# 检查工具三项改进 — 实施计划

> **For agentic workers:** 按 Task 顺序逐个执行，每个 Task 内的 Step 也按顺序执行。步骤使用 checkbox (`- [ ]`) 语法追踪进度。

**目标：** 草稿扩容6条+可删除、AI优化可取消/可跳过、导入.docx和照片

**架构：** 纯前端 SPA，改动集中在 db.js（存储层）、ui.js（视图层）、app.js（协调层），新增 importer.js（导入逻辑）

**技术栈：** IndexedDB + Vanilla JS (ES Modules) + JSZip + 豆包 API

---

## 文件改动地图

```
inspection-tool/
├── index.html              # 引入 JSZip
├── lib/jszip.min.js        # NEW — 本地化 JSZip
├── js/
│   ├── db.js               # MODIFY — schema v2, 6条限制, ID keyPath
│   ├── app.js              # MODIFY — 适配新草案结构, AbortController, 导入协调
│   ├── ui.js               # MODIFY — 草稿卡片, 取消/跳过按钮, 导入面板
│   ├── ai.js               # MODIFY — signal 参数
│   ├── importer.js         # NEW — Word解析 + 照片OCR + 合并逻辑
│   ├── docx-gen.js         # UNCHANGED
│   ├── camera-voice.js     # UNCHANGED
└── lib/docx.umd.js         # UNCHANGED
```

---

### Task 1: db.js — 数据库 schema 升级到 v2

**文件:**
- 修改: `js/db.js`

**功能:** keyPath 从 `type` 改为 `id`，支持最多 6 条草稿，自动清理最旧记录。

- [ ] **Step 1: 重写 db.js 完整内容**

用以下内容替换整个 `js/db.js`：

```js
// db.js — IndexedDB 草稿存储 + localStorage 预设
// v2: keyPath 改为 id，支持最多 6 条草稿

const DB_NAME = 'inspection-tool';
const DB_VERSION = 2;
const STORE_NAME = 'drafts';
const MAX_DRAFTS = 6;

// ---------- ID 生成 ----------

function generateId() {
  return 'draft_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8);
}

// ---------- IndexedDB ----------

function openDB() {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);

    req.onupgradeneeded = (e) => {
      const db = e.target.result;
      const oldVersion = e.oldVersion;

      if (oldVersion < 1) {
        // 首次创建：直接用新 schema (keyPath: id)
        db.createObjectStore(STORE_NAME, { keyPath: 'id' });
      }

      if (oldVersion === 1) {
        // v1 → v2：删除旧 store (keyPath: type)，创建新 store (keyPath: id)
        // 旧数据已在 migrateFromV1() 中提前备份，这里直接重建
        if (db.objectStoreNames.contains(STORE_NAME)) {
          db.deleteObjectStore(STORE_NAME);
        }
        db.createObjectStore(STORE_NAME, { keyPath: 'id' });
      }
    };

    req.onsuccess = (e) => resolve(e.target.result);
    req.onerror = (e) => reject(e.target.error);
  });
}

// v1 → v2 迁移辅助：尝试从 v1 格式读取旧数据
async function migrateFromV1() {
  // 检查是否已经迁移过
  if (localStorage.getItem('draft_migration_done') === 'v2') return;

  try {
    // 用 v1 的方式打开数据库读取旧数据
    const v1Data = await readV1Data();
    if (v1Data.length > 0) {
      const db = await openDB();
      const tx = db.transaction(STORE_NAME, 'readwrite');
      const store = tx.objectStore(STORE_NAME);
      for (const old of v1Data) {
        store.put({
          id: generateId(),
          type: old.type,        // 保留原始 type
          data: old.data,
          updatedAt: old.updatedAt || Date.now(),
        });
      }
      await new Promise((resolve, reject) => {
        tx.oncomplete = () => resolve();
        tx.onerror = (e) => reject(e.target.error);
      });
    }
    localStorage.setItem('draft_migration_done', 'v2');
  } catch (e) {
    console.warn('[DB] 迁移失败（如果无旧数据可忽略）:', e.message);
    localStorage.setItem('draft_migration_done', 'v2');
  }
}

// 用 v1 方式读取旧数据
function readV1Data() {
  return new Promise((resolve) => {
    // 打开 v1 数据库只读（不触发 onupgradeneeded）
    const req = indexedDB.open(DB_NAME, 1);
    req.onupgradeneeded = () => {
      // v1 不存在，直接拒绝
    };
    req.onsuccess = (e) => {
      const db = e.target.result;
      // 检查旧 store 是否还存在且 keyPath 是 type
      if (!db.objectStoreNames.contains(STORE_NAME)) {
        db.close();
        resolve([]);
        return;
      }
      try {
        const tx = db.transaction(STORE_NAME, 'readonly');
        const store = tx.objectStore(STORE_NAME);
        const getAllReq = store.getAll();
        getAllReq.onsuccess = () => {
          db.close();
          resolve(getAllReq.result || []);
        };
        getAllReq.onerror = () => {
          db.close();
          resolve([]);
        };
      } catch (e) {
        db.close();
        resolve([]);
      }
    };
    req.onerror = () => resolve([]);
  });
}

async function saveDraft(type, data) {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readwrite');
  const store = tx.objectStore(STORE_NAME);

  const draft = {
    id: generateId(),
    type,
    data,
    updatedAt: Date.now(),
  };

  store.put(draft);

  // 限制最多 MAX_DRAFTS 条
  const all = await new Promise((resolve, reject) => {
    const getAllReq = store.getAll();
    getAllReq.onsuccess = () => resolve(getAllReq.result || []);
    getAllReq.onerror = (e) => reject(e.target.error);
  });

  if (all.length > MAX_DRAFTS) {
    // 按 updatedAt 升序，删除最旧的
    const sorted = all.sort((a, b) => a.updatedAt - b.updatedAt);
    const toDelete = sorted.slice(0, all.length - MAX_DRAFTS);
    for (const d of toDelete) {
      store.delete(d.id);
    }
  }

  return new Promise((resolve, reject) => {
    tx.oncomplete = () => resolve(draft.id);
    tx.onerror = (e) => reject(e.target.error);
  });
}

async function getDraft(id) {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readonly');
  const store = tx.objectStore(STORE_NAME);
  const req = store.get(id);
  return new Promise((resolve, reject) => {
    req.onsuccess = () => resolve(req.result ? req.result.data : null);
    req.onerror = (e) => reject(e.target.error);
  });
}

async function deleteDraft(id) {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readwrite');
  const store = tx.objectStore(STORE_NAME);
  store.delete(id);
  return new Promise((resolve, reject) => {
    tx.oncomplete = () => resolve();
    tx.onerror = (e) => reject(e.target.error);
  });
}

async function listDrafts() {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readonly');
  const store = tx.objectStore(STORE_NAME);
  const req = store.getAll();
  return new Promise((resolve, reject) => {
    req.onsuccess = () => {
      // 按 updatedAt 降序排列
      const result = req.result || [];
      result.sort((a, b) => b.updatedAt - a.updatedAt);
      resolve(result);
    };
    req.onerror = (e) => reject(e.target.error);
  });
}

// ---------- localStorage ----------

const PRESETS_KEY = 'inspection-presets';

function getPresets() {
  try {
    const raw = localStorage.getItem(PRESETS_KEY);
    if (raw) return JSON.parse(raw);
  } catch (e) { /* ignore */ }
  return {
    company: '广西糖业集团红河制糖有限公司',
    department: '压榨车间'
  };
}

function savePresets(presets) {
  localStorage.setItem(PRESETS_KEY, JSON.stringify(presets));
}

function getTodayStr() {
  const d = new Date();
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
}

export { saveDraft, getDraft, deleteDraft, listDrafts, getPresets, savePresets, getTodayStr, MAX_DRAFTS, migrateFromV1 };
```

- [ ] **Step 2: 验证 db.js 无语法错误**

Run: `node --check js/db.js`
Expected: 无输出（表示语法通过）

- [ ] **Step 3: Commit**

```bash
git add js/db.js
git commit -m "feat: 草稿系统升级 — keyPath改为id, 支持6条草稿, v1→v2迁移"
```

---

### Task 2: ui.js — 首页草稿卡片改造

**文件:**
- 修改: `js/ui.js` — `renderHomePage` 函数

**功能:** 草稿卡片显示类型标签、日期、删除按钮；容量指示。

- [ ] **Step 1: 修改 renderHomePage 的草稿区代码**

在 `js/ui.js` 的 `renderHomePage` 函数中，找到草稿区渲染部分（约第 64-82 行），替换为以下代码。

定位标记：在 `let draftsHtml = '';` 那一行，替换整段草稿渲染逻辑。

将这段：
```js
  let draftsHtml = '';
  if (drafts && drafts.length > 0) {
    const labels = { safety: '安全自查', '5s': '现场管理', company: '公司检查' };
    draftsHtml = `
      <div style="margin-top:16px;">
        <h3 style="font-size:14px;color:var(--text-secondary);margin-bottom:8px;">📝 未完成的草稿</h3>
        ${drafts.map(d => `
          <div class="card" data-action="resume" data-type="${d.type}" style="display:flex;align-items:center;gap:10px;">
            <span style="font-size:24px;">📄</span>
            <div style="flex:1;">
              <div style="font-weight:600;">${labels[d.type] || d.type}</div>
              <div style="font-size:12px;color:#999;">${d.data?.items?.length || 0} 条记录 · ${new Date(d.updatedAt).toLocaleDateString('zh-CN')}</div>
            </div>
            <span style="color:var(--primary);font-size:13px;">继续 ></span>
          </div>
        `).join('')}
      </div>
    `;
  }
```

替换为：
```js
  let draftsHtml = '';
  if (drafts && drafts.length > 0) {
    const labels = { safety: '安全自查', '5s': '现场管理', company: '公司检查' };
    const typeColors = { safety: '#4A90D9', '5s': '#F5A623', company: '#7ED321' };
    const typeNames = { safety: '安全', '5s': '5S', company: '公司' };
    // 从 db.js 导入 MAX_DRAFTS，这里硬编码 6 避免循环依赖
    draftsHtml = `
      <div style="margin-top:16px;">
        <h3 style="font-size:14px;color:var(--text-secondary);margin-bottom:8px;">📝 草稿箱 (${drafts.length}/6)</h3>
        ${drafts.map(d => `
          <div class="card draft-card" style="display:flex;align-items:center;gap:10px;position:relative;border-left:4px solid ${typeColors[d.type] || '#ccc'};">
            <span style="background:${typeColors[d.type] || '#ccc'};color:#fff;font-size:10px;padding:2px 8px;border-radius:10px;flex-shrink:0;">${typeNames[d.type] || d.type}</span>
            <div style="flex:1;min-width:0;" data-action="resume" data-id="${d.id}" data-type="${d.type}">
              <div style="font-weight:600;font-size:14px;">${labels[d.type] || d.type}</div>
              <div style="font-size:12px;color:#999;">${d.data?.items?.length || 0} 条记录 · ${new Date(d.updatedAt).toLocaleDateString('zh-CN')}</div>
            </div>
            <button class="draft-delete-btn" data-action="delete-draft" data-id="${d.id}" style="background:none;border:none;font-size:18px;cursor:pointer;padding:6px 8px;color:#ccc;flex-shrink:0;" title="删除草稿">🗑️</button>
          </div>
        `).join('')}
      </div>
    `;
  }
```

- [ ] **Step 2: 修改首页点击事件处理，增加删除草稿逻辑**

在 `renderHomePage` 函数的事件监听部分（约第 105-114 行），替换为：

```js
  document.getElementById('home-page').addEventListener('click', (e) => {
    // 删除草稿按钮
    const delBtn = e.target.closest('[data-action="delete-draft"]');
    if (delBtn) {
      e.stopPropagation();
      const draftId = delBtn.dataset.id;
      if (confirm('确定删除这条草稿吗？')) {
        import('./db.js?v=' + Date.now()).then(({ deleteDraft, listDrafts }) => {
          deleteDraft(draftId).then(() => {
            listDrafts().then(newDrafts => {
              renderHomePage({ drafts: newDrafts, onSelectType });
            });
          });
        }).catch(() => {
          // 如果动态 import 失败，刷新页面
          location.reload();
        });
      }
      return;
    }

    const card = e.target.closest('[data-action]');
    if (!card) return;
    const action = card.dataset.action;
    if (action === 'select-type') {
      onSelectType(card.dataset.type);
    } else if (action === 'resume') {
      onSelectType(card.dataset.type, true, card.dataset.id);
    }
  });
```

- [ ] **Step 3: 同步更新 app.js 的 showHome onSelectType 回调签名**

`renderHomePage` 中的 `onSelectType` 现在传第三个参数 `draftId`。等 Task 3 会在 app.js 中适配。

- [ ] **Step 4: 验证语法**

Run: `node --check js/ui.js`
Expected: 无输出

- [ ] **Step 5: Commit**

```bash
git add js/ui.js
git commit -m "feat: 草稿卡片改造 — 类型标签+删除按钮+容量指示"
```

---

### Task 3: app.js — 适配新草稿数据结构

**文件:**
- 修改: `js/app.js`

**功能:** `showHome` 调用迁移；`onSelectType` 接受新 draftId 参数；`showItemList` 中用旧 type 参数兼容新建草稿。

- [ ] **Step 1: 修改 showHome 函数，初始化时执行迁移**

在 `showHome` 函数开头增加迁移调用（约第 35 行）：

```js
function showHome() {
  // v1 → v2 迁移（首次运行时执行）
  import('./db.js?v=20260701a').then(({ migrateFromV1 }) => {
    migrateFromV1().then(() => {
      listDrafts().then(drafts => {
        renderHomePage({ drafts, onSelectType });
      });
    });
  }).catch(() => {
    listDrafts().then(drafts => {
      renderHomePage({ drafts, onSelectType });
    });
  });
}
```

- [ ] **Step 2: 修改 onSelectType 回调，接受新 draftId 参数**

在 `showHome` 函数内，修改 `onSelectType` 回调（约第 38-63 行）：

```js
    onSelectType: (type, resume, draftId) => {
      state.reportType = type;

      const defaults = {
        company: FIXED_COMPANY,
        department: FIXED_DEPARTMENT,
        date: getTodayStr(),
        inspectionDate: getTodayStr(),
        halfMonth: type === '5s' ? 'first' : null,
      };

      if (resume && draftId) {
        getDraft(draftId).then(draftData => {
          if (draftData) {
            state.items = draftData.items || [];
            state.headerInfo = { ...defaults, ...draftData.headerInfo };
            state.currentDraftId = draftId;  // 记住当前草稿 ID
          }
          showItemList();
        });
      } else {
        state.items = [];
        state.headerInfo = defaults;
        state.currentDraftId = null;
        showItemList();
      }
    },
```

- [ ] **Step 3: 修改 showItemList 的保存草稿逻辑，保留 draft 的 type 信息**

在 `showItemList` 函数中，修改 `saveCurrentDraft`（约第 70-77 行）：

```js
    const saveCurrentDraft = () => {
      if (state.reportType && state.items.length > 0) {
        saveDraft(state.reportType, {
          items: state.items,
          headerInfo: state.headerInfo,
        }).then(newId => {
          state.currentDraftId = newId;
        }).catch(e => console.error('保存草稿失败:', e));
      }
    };
```

- [ ] **Step 4: 在 state 中增加 currentDraftId 字段**

在全局 state 对象中（约第 17-28 行）增加字段：

```js
const state = {
  reportType: null,
  items: [],
  currentDraftId: null,  // 新增：当前编辑的草稿 ID
  headerInfo: {
    // ... 保持不变
  },
  currentPage: 'home',
};
```

- [ ] **Step 5: 修改 showItemForm 的保存调用**

在 `showItemForm` 函数中（约第 119-124 行），确认 `saveDraft` 调用传了 `state.reportType`：

```js
      if (state.reportType) {
        saveDraft(state.reportType, {
          items: state.items,
          headerInfo: state.headerInfo,
        }).then(newId => {
          state.currentDraftId = newId;
        }).catch(e => console.error('保存草稿失败:', e));
      }
```

- [ ] **Step 6: 验证语法**

Run: `node --check js/app.js`
Expected: 无输出

- [ ] **Step 7: Commit**

```bash
git add js/app.js
git commit -m "feat: 适配新草稿数据结构 — draftId追踪, v1迁移, 保存返回新ID"
```

---

### Task 4: ai.js — callDoubaoOptimize 增加 signal 参数

**文件:**
- 修改: `js/ai.js` — `callDoubaoOptimize` 函数

**功能:** 接受 AbortSignal，支持取消请求。

- [ ] **Step 1: 修改 callDoubaoOptimize 函数签名和 fetch 调用**

在 `js/ai.js` 中，找到 `callDoubaoOptimize` 函数（约第 44-96 行），修改函数签名和 fetch 调用：

将：
```js
async function callDoubaoOptimize(text, reportType) {
  const response = await fetch(DOUBAO_API_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${DOUBAO_API_KEY}`
    },
    body: JSON.stringify({
```

改为：
```js
async function callDoubaoOptimize(text, reportType, signal) {
  const response = await fetch(DOUBAO_API_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${DOUBAO_API_KEY}`
    },
    signal,  // 支持 AbortController 取消
    body: JSON.stringify({
```

其余函数体不变。

- [ ] **Step 2: 验证语法**

Run: `node --check js/ai.js`
Expected: 无输出

- [ ] **Step 3: Commit**

```bash
git add js/ai.js
git commit -m "feat: AI优化支持取消 — callDoubaoOptimize增加signal参数"
```

---

### Task 5: ui.js — AI 优化页增加取消按钮和使用原文按钮

**文件:**
- 修改: `js/ui.js` — `renderOptimizePage` 函数

**功能:** 
1. 加载态显示取消按钮
2. 结果页显示"使用原文"按钮
3. 回调签名调整

- [ ] **Step 1: 修改 renderOptimizePage 函数签名，增加 onUseOriginal 和 onCancel 回调**

在 `js/ui.js` 中找到 `renderOptimizePage` 函数（约第 394 行），将函数签名改为：

```js
function renderOptimizePage({ text, reportType, options, loading, onSelect, onEdit, onRetry, onBack, onUseOriginal, onCancel }) {
```

- [ ] **Step 2: 修改加载态 HTML，增加取消按钮**

在 `renderOptimizePage` 函数内部，修改加载态的渲染。当前是调用方（app.js）手动设置 loading HTML，这里改成由 renderOptimizePage 自己根据 `loading` 参数渲染。

找到模板中 id 为 `options-container` 的 div 部分（约第 409-415 行），替换为：

```js
      <p style="font-size:13px;color:var(--text-secondary);margin-bottom:8px;">请选择一个优化结果：</p>
      <div id="options-container">
        ${loading ? `
          <div style="text-align:center;padding:40px;">
            <span class="spinner" style="width:32px;height:32px;"></span>
            <p style="margin-top:12px;color:var(--text-secondary);">AI 正在优化描述...</p>
            <button class="btn btn-outline" id="cancel-optimize-btn" style="margin-top:12px;color:#e74c3c;border-color:#e74c3c;">✕ 取消</button>
          </div>
        ` : options.map((opt, i) => `
          <div class="option-card" data-index="${i}" id="option-${i}">
            <div style="font-weight:500;">${String.fromCharCode(65 + i)}. ${escapeHtml(opt)}</div>
          </div>
        `).join('')}
      </div>
      <div style="display:flex;gap:10px;margin-top:14px;">
        ${!loading ? `
          <button class="btn btn-warning btn-block" id="edit-selected-btn" disabled>✏️ 编辑修改</button>
          <button class="btn btn-purple btn-block" id="retry-btn">🔄 换一批</button>
        ` : ''}
      </div>
      ${!loading ? `
        <button class="btn" id="use-original-btn" style="width:100%;margin-top:10px;padding:10px;border-radius:8px;border:1px solid #999;background:#fff;color:#666;font-size:14px;">📋 直接使用原文（不用 AI 结果）</button>
      ` : ''}
```

- [ ] **Step 3: 绑定取消按钮和原文按钮的事件**

在 `renderOptimizePage` 函数的事件绑定区（约第 423-437 行），增加新按钮的绑定：

```js
  // 取消按钮
  const cancelBtn = document.getElementById('cancel-optimize-btn');
  if (cancelBtn && onCancel) {
    cancelBtn.onclick = () => onCancel();
  }

  // 使用原文按钮
  const useOriginalBtn = document.getElementById('use-original-btn');
  if (useOriginalBtn && onUseOriginal) {
    useOriginalBtn.onclick = () => onUseOriginal(text);
  }

  // ... 保留原有的选项点击、编辑、换一批逻辑
  let selectedIndex = -1;
  document.getElementById('optimize-back').onclick = onBack;

  if (!loading) {
    document.getElementById('options-container').addEventListener('click', (e) => {
      const card = e.target.closest('.option-card');
      if (!card) return;
      document.querySelectorAll('.option-card').forEach(c => c.classList.remove('selected'));
      card.classList.add('selected');
      selectedIndex = parseInt(card.dataset.index);
      document.getElementById('edit-selected-btn').disabled = false;
    });
    document.getElementById('edit-selected-btn').onclick = () => {
      if (selectedIndex >= 0) onEdit(options[selectedIndex]);
    };
    document.getElementById('retry-btn').onclick = () => onRetry();
  }
```

- [ ] **Step 4: 验证语法**

Run: `node --check js/ui.js`
Expected: 无输出

- [ ] **Step 5: Commit**

```bash
git add js/ui.js
git commit -m "feat: AI优化页增加取消按钮+使用原文按钮"
```

---

### Task 6: app.js — showOptimizePage 重构，支持取消和跳过

**文件:**
- 修改: `js/app.js` — `showOptimizePage` 函数

**功能:** 创建 AbortController、处理取消、处理使用原文。

- [ ] **Step 1: 完全重写 showOptimizePage 函数**

在 `js/app.js` 中，找到 `showOptimizePage` 函数（约第 134-197 行），用以下完整代码替换：

```js
// ---------- AI 润色 ----------

async function showOptimizePage(text, editIndex) {
  // 暂存当前表单照片，防止返回时丢失
  const photoOverride = {
    beforePhoto: window._formBeforePhoto,
    afterPhoto: window._formAfterPhoto,
  };

  // 创建 AbortController，用于取消请求
  let abortController = new AbortController();
  let cancelled = false;

  // 加载态
  renderOptimizePage({
    text,
    reportType: state.reportType,
    options: [],
    loading: true,
    onSelect: () => {}, onEdit: () => {}, onRetry: () => {},
    onUseOriginal: () => {}, onBack: () => {},
    onCancel: () => {
      cancelled = true;
      abortController.abort();
      showItemForm(editIndex, photoOverride);
    },
  });

  try {
    const options = await callDoubaoOptimize(text, state.reportType, abortController.signal);

    if (cancelled) return; // 已取消，不渲染结果

    renderOptimizePage({
      text,
      reportType: state.reportType,
      options,
      loading: false,
      onSelect: (selectedText) => {
        window._optimizedText = selectedText;
        showItemForm(editIndex, photoOverride);
        setTimeout(() => {
          const descEl = document.getElementById('item-desc');
          if (descEl && window._optimizedText) {
            descEl.value = window._optimizedText;
            descEl.dispatchEvent(new Event('input'));
            delete window._optimizedText;
          }
        }, 100);
      },
      onEdit: (selectedText) => {
        showEditModal(selectedText, (editedText) => {
          window._optimizedText = editedText;
          showItemForm(editIndex, photoOverride);
          setTimeout(() => {
            const descEl = document.getElementById('item-desc');
            if (descEl && window._optimizedText) {
              descEl.value = window._optimizedText;
              delete window._optimizedText;
            }
          }, 100);
        });
      },
      onRetry: () => showOptimizePage(text, editIndex),
      onUseOriginal: (originalText) => {
        // 直接用原文，等同选择了原文
        window._optimizedText = originalText;
        showItemForm(editIndex, photoOverride);
        setTimeout(() => {
          const descEl = document.getElementById('item-desc');
          if (descEl && window._optimizedText) {
            descEl.value = window._optimizedText;
            descEl.dispatchEvent(new Event('input'));
            delete window._optimizedText;
          }
        }, 100);
      },
      onCancel: () => {
        cancelled = true;
        abortController.abort();
        showItemForm(editIndex, photoOverride);
      },
      onBack: () => showItemForm(editIndex, photoOverride),
    });
  } catch (e) {
    if (e.name === 'AbortError' || cancelled) {
      // 用户主动取消，静默返回
      return;
    }
    showToast('网络异常，请检查网络后重试');
    showItemForm(editIndex, photoOverride);
  }
}
```

- [ ] **Step 2: 验证语法**

Run: `node --check js/app.js`
Expected: 无输出

- [ ] **Step 3: Commit**

```bash
git add js/app.js
git commit -m "feat: AI优化重构 — AbortController取消 + 使用原文跳过"
```

---

### Task 7: 本地化 JSZip 库

**文件:**
- 新建: `lib/jszip.min.js`
- 修改: `index.html`

**功能:** 将 JSZip 下载到本地 lib 目录，避免国内 CDN 加载慢。

- [ ] **Step 1: 下载 JSZip**

Run: `curl -L -o lib/jszip.min.js https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js`

如果 curl 失败，手动从 https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js 下载放到 `lib/jszip.min.js`。

- [ ] **Step 2: 在 index.html 中引入 JSZip**

在 `index.html` 的 `<script src="lib/docx.umd.js"></script>` 之后加一行：

```html
  <script src="lib/jszip.min.js"></script>
```

完整 `<body>` 底部：

```html
  <!-- docx.js 本地文件，避免国外 CDN 慢 -->
  <script src="lib/docx.umd.js"></script>
  <!-- JSZip 本地文件，用于 .docx 解析导入 -->
  <script src="lib/jszip.min.js"></script>

  <!-- 应用模块 -->
  <script type="module" src="js/app.js?v=20260701a"></script>
```

- [ ] **Step 3: 验证 JSZip 可加载**

在 `index.html` 的 `<script type="module">` 之前加一段测试（上线前删除）：

```html
  <script>
    if (typeof JSZip === 'undefined') {
      console.error('JSZip 加载失败');
    } else {
      console.log('JSZip 加载成功, version:', JSZip.version);
    }
  </script>
```

- [ ] **Step 4: Commit**

```bash
git add lib/jszip.min.js index.html
git commit -m "chore: 本地化 JSZip 3.10.1，用于 .docx 导入解析"
```

---

### Task 8: importer.js — Word .docx 解析器

**文件:**
- 新建: `js/importer.js`

**功能:** 解析本工具生成的 .docx 文件，提取问题描述和图片。

- [ ] **Step 1: 创建 importer.js**

新建 `js/importer.js`，完整内容：

```js
// importer.js — .docx 导入解析 + 照片 OCR
// 依赖全局 JSZip 对象（index.html 中引入）

/**
 * 解析 .docx 文件，提取问题条目
 * 只解析本工具生成的格式（表格结构已知）
 * @param {File} file — .docx 文件
 * @returns {Promise<{items: Array, reportType: string|null}>}
 */
async function parseDocx(file) {
  // 1. 读取文件
  const buffer = await file.arrayBuffer();
  const zip = await JSZip.loadAsync(buffer);

  // 2. 读取 document.xml
  const docXml = await zip.file('word/document.xml')?.async('string');
  if (!docXml) {
    throw new Error('无法读取文档内容，请确认是有效的 .docx 文件');
  }

  // 3. 解析 XML
  const parser = new DOMParser();
  const xmlDoc = parser.parseFromString(docXml, 'text/xml');

  // 检查是否有解析错误
  const parseError = xmlDoc.querySelector('parsererror');
  if (parseError) {
    throw new Error('文档 XML 解析失败，文件可能已损坏');
  }

  // 4. 提取所有表格行（跳过表头第一行）
  const rows = xmlDoc.querySelectorAll('w\\:tbl w\\:tr, tbl tr');
  if (rows.length < 2) {
    throw new Error('未在文档中找到问题条目。仅支持本工具生成的报告格式');
  }

  // 5. 提取图片关系映射
  const relsXml = await zip.file('word/_rels/document.xml.rels')?.async('string');
  const imageMap = {};
  if (relsXml) {
    const relsDoc = parser.parseFromString(relsXml, 'text/xml');
    const relationships = relsDoc.querySelectorAll('Relationship');
    relationships.forEach(rel => {
      const id = rel.getAttribute('Id');
      const target = rel.getAttribute('Target');
      if (target && target.startsWith('media/')) {
        imageMap[id] = target;
      }
    });
  }

  // 6. 逐行解析
  const items = [];
  // 跳过表头（第一行）
  for (let i = 1; i < rows.length; i++) {
    const row = rows[i];
    const cells = row.querySelectorAll('w\\:tc, tc');

    const item = {
      description: '',
      beforePhoto: '',
      afterPhoto: '',
      status: '待整改',
    };

    // 确定列映射：根据列数判断报告类型
    const cellCount = cells.length;

    if (cellCount === 6) {
      // safety 类型：序号/部门/问题描述/整改前图片/整改后图片/备注
      item.description = extractCellText(cells[2]);
      item.beforePhoto = await extractCellImage(cells[3], zip, imageMap);
      item.afterPhoto = await extractCellImage(cells[4], zip, imageMap);
      const remark = extractCellText(cells[5]);
      if (remark.includes('已整改')) item.status = '已整改';
    } else if (cellCount === 5) {
      // 5s/company 类型：序号/存在问题/整改前图片/整改后图片/备注
      item.description = extractCellText(cells[1]);
      item.beforePhoto = await extractCellImage(cells[2], zip, imageMap);
      item.afterPhoto = await extractCellImage(cells[3], zip, imageMap);
      const remark = extractCellText(cells[4]);
      if (remark.includes('已整改')) item.status = '已整改';
    } else {
      // 跳过不认识的格式
      continue;
    }

    // 跳过完全空行
    if (!item.description && !item.beforePhoto && !item.afterPhoto) continue;

    items.push(item);
  }

  if (items.length === 0) {
    throw new Error('未从文档中提取到问题条目。仅支持本工具生成的报告格式');
  }

  // 7. 尝试检测报告类型
  let reportType = null;
  const docText = docXml.replace(/<[^>]+>/g, ' '); // 去标签取纯文本
  if (docText.includes('安全自检自查') || docText.includes('安全自查')) {
    reportType = 'safety';
  } else if (docText.includes('5S') || docText.includes('5s')) {
    reportType = '5s';
  } else if (docText.includes('现场整改')) {
    reportType = 'company';
  }

  return { items, reportType };
}

/**
 * 提取单元格中的纯文本
 */
function extractCellText(cell) {
  if (!cell) return '';
  // 获取所有文本元素
  const texts = cell.querySelectorAll('w\\:t, t');
  return Array.from(texts).map(t => t.textContent || '').join('').trim();
}

/**
 * 提取单元格中的图片，转为 base64 data URL
 */
async function extractImageCell(cell, zip, imageMap) {
  if (!cell) return '';

  // 查找图片引用
  const blips = cell.querySelectorAll('a\\:blip, blip');
  for (const blip of blips) {
    const embed = blip.getAttribute('r:embed') || blip.getAttribute('embed');
    if (!embed) continue;

    const mediaPath = imageMap[embed];
    if (!mediaPath) continue;

    // 从 zip 中读取图片
    const imageFile = zip.file('word/' + mediaPath);
    if (!imageFile) continue;

    const imageData = await imageFile.async('base64');
    // 检测图片类型
    const ext = mediaPath.split('.').pop().toLowerCase();
    const mimeMap = { jpg: 'jpeg', jpeg: 'jpeg', png: 'png', gif: 'gif', bmp: 'bmp', webp: 'webp' };
    const mime = mimeMap[ext] || 'jpeg';

    return `data:image/${mime};base64,${imageData}`;
  }

  return '';
}

export { parseDocx };
```

- [ ] **Step 2: 验证语法**

Run: `node --check js/importer.js`
Expected: 无输出

- [ ] **Step 3: Commit**

```bash
git add js/importer.js
git commit -m "feat: 新建importer.js — .docx文件解析, 提取描述和图片"
```

---

### Task 9: ui.js + app.js — 导入入口和合并面板

**文件:**
- 修改: `js/ui.js` — 导入按钮 + 合并选择面板
- 修改: `js/app.js` — 导入流程协调

**功能:** 首页增加导入按钮；导入后弹出合并目标选择面板；合并逻辑。

- [ ] **Step 1: 在 renderHomePage 中增加导入按钮**

在 `js/ui.js` 的 `renderHomePage` 函数模板中，在类型卡片列表之前，插入导入按钮。找到 `typeCards.map` 的那行（约第 93 行），在前面加：

```js
      <div class="card import-card" data-action="import-file" style="border:2px dashed var(--primary);text-align:center;padding:14px;background:#f0f7ff;">
        <div style="font-size:28px;margin-bottom:4px;">📥</div>
        <div style="font-weight:600;color:var(--primary);">导入报告文件</div>
        <div style="font-size:11px;color:#999;">支持 Word 报告 (.docx) 或照片</div>
      </div>
```

并在事件处理中增加对应的 action：

```js
    if (action === 'import-file') {
      // 触发文件选择
      const input = document.createElement('input');
      input.type = 'file';
      input.accept = '.docx,image/*';
      input.onchange = (e) => {
        const file = e.target.files[0];
        if (!file) return;
        // 判断是 docx 还是图片
        if (file.name.endsWith('.docx')) {
          onSelectType('__import_docx__', false, null, file);
        } else if (file.type.startsWith('image/')) {
          onSelectType('__import_photo__', false, null, file);
        }
      };
      input.click();
      return;
    }
```

- [ ] **Step 2: 修改 onSelectType 回调，处理导入文件**

在 app.js 的 `showHome` 函数中，修改 `onSelectType` 回调，增加对 `__import_docx__` 和 `__import_photo__` 的处理：

```js
    onSelectType: (type, resume, draftId, file) => {
      // 处理导入 .docx
      if (type === '__import_docx__' && file) {
        handleImportDocx(file);
        return;
      }
      // 处理导入照片
      if (type === '__import_photo__' && file) {
        handleImportPhoto(file);
        return;
      }

      // 原有逻辑...
      state.reportType = type;
      // ...
    },
```

- [ ] **Step 3: 新增 showMergePanel 函数到 ui.js**

在 `js/ui.js` 末尾（export 之前）增加合并面板函数：

```js
/**
 * 显示导入合并目标选择面板
 * @param {Array} parsed — 解析结果 { items, reportType }
 * @param {Array} drafts — 所有草稿列表
 * @param {Function} onConfirm — 确认回调 (targetDraftId|null, reportType)
 * @param {Function} onCancel — 取消回调
 */
function showMergePanel({ parsed, drafts, onConfirm, onCancel }) {
  const recentDraft = drafts.length > 0 ? drafts[0] : null; // 已按时间降序

  const overlay = document.createElement('div');
  overlay.style.cssText = 'position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:50;display:flex;align-items:flex-end;justify-content:center;';
  overlay.innerHTML = `
    <div style="background:#fff;width:100%;max-width:480px;border-radius:16px 16px 0 0;padding:20px;max-height:80vh;overflow-y:auto;">
      <h3 style="margin-bottom:4px;">📥 导入预览</h3>
      <p style="font-size:13px;color:#999;margin-bottom:14px;">识别到 <strong>${parsed.items.length}</strong> 条问题</p>

      <p style="font-size:13px;color:#999;margin-bottom:8px;">选择导入目标：</p>

      ${recentDraft ? `
        <div class="merge-option selected" data-target="recent" style="border:2px solid var(--primary);border-radius:10px;padding:12px;margin-bottom:8px;background:#f0f7ff;">
          <div style="display:flex;align-items:center;gap:8px;">
            <span style="background:var(--primary);color:#fff;border-radius:50%;width:22px;height:22px;display:inline-flex;align-items:center;justify-content:center;font-size:12px;flex-shrink:0;">✓</span>
            <div>
              <div style="font-weight:600;">合并到最近编辑的草稿</div>
              <div style="font-size:12px;color:#999;">${recentDraft.data?.items?.length || 0} 条 · ${new Date(recentDraft.updatedAt).toLocaleDateString('zh-CN')}</div>
            </div>
          </div>
        </div>
      ` : ''}

      ${drafts.filter(d => d !== recentDraft).map(d => `
        <div class="merge-option" data-target="${d.id}" style="border:1px solid #e0e0e0;border-radius:10px;padding:12px;margin-bottom:8px;">
          <div style="display:flex;align-items:center;gap:8px;">
            <span style="border:1px solid #ccc;color:#ccc;border-radius:50%;width:22px;height:22px;display:inline-flex;align-items:center;justify-content:center;font-size:12px;flex-shrink:0;">○</span>
            <div>
              <div style="font-weight:600;font-size:14px;">合并到其他草稿</div>
              <div style="font-size:12px;color:#999;">${d.data?.items?.length || 0} 条 · ${new Date(d.updatedAt).toLocaleDateString('zh-CN')}</div>
            </div>
          </div>
        </div>
      `).join('')}

      <div class="merge-option" data-target="new" style="border:1px solid #e0e0e0;border-radius:10px;padding:12px;margin-bottom:14px;">
        <div style="display:flex;align-items:center;gap:8px;">
          <span style="border:1px solid #ccc;color:#ccc;border-radius:50%;width:22px;height:22px;display:inline-flex;align-items:center;justify-content:center;font-size:12px;flex-shrink:0;">○</span>
          <div>
            <div style="font-weight:600;font-size:14px;">创建新草稿</div>
            <div style="font-size:12px;color:#999;">不合并，单独保存</div>
          </div>
        </div>
      </div>

      <button class="btn btn-primary btn-block" id="merge-confirm-btn" style="font-size:16px;">确认导入</button>
      <button class="btn btn-outline btn-block" id="merge-cancel-btn" style="margin-top:8px;">取消</button>
    </div>`;

  document.body.appendChild(overlay);

  let selectedTarget = recentDraft ? 'recent' : 'new';

  // 选项点击切换
  overlay.querySelectorAll('.merge-option').forEach(opt => {
    opt.addEventListener('click', () => {
      overlay.querySelectorAll('.merge-option').forEach(o => {
        o.classList.remove('selected');
        o.style.border = '1px solid #e0e0e0';
        o.style.background = '#fff';
        const check = o.querySelector('span');
        check.style.background = 'transparent';
        check.style.color = '#ccc';
        check.style.border = '1px solid #ccc';
        check.textContent = '○';
      });
      opt.classList.add('selected');
      opt.style.border = '2px solid var(--primary)';
      opt.style.background = '#f0f7ff';
      const check = opt.querySelector('span');
      check.style.background = 'var(--primary)';
      check.style.color = '#fff';
      check.style.border = 'none';
      check.textContent = '✓';
      selectedTarget = opt.dataset.target;
    });
  });

  overlay.querySelector('#merge-confirm-btn').onclick = () => {
    let targetDraftId = null;
    if (selectedTarget === 'new') {
      targetDraftId = null;
    } else if (selectedTarget === 'recent') {
      targetDraftId = recentDraft ? recentDraft.id : null;
    } else {
      targetDraftId = selectedTarget;
    }
    overlay.remove();
    onConfirm(targetDraftId, parsed.reportType || 'safety');
  };

  overlay.querySelector('#merge-cancel-btn').onclick = () => {
    overlay.remove();
    onCancel();
  };

  overlay.addEventListener('click', (e) => {
    if (e.target === overlay) { overlay.remove(); onCancel(); }
  });
}
```

并在文件末尾的 export 中增加 `showMergePanel`。

- [ ] **Step 4: 在 app.js 中增加 handleImportDocx 函数**

在 `js/app.js` 的 `showHome` 之前增加导入处理函数：

```js
// ---------- 导入 .docx ----------

async function handleImportDocx(file) {
  showToast('正在解析文件...');

  let parsed;
  try {
    const { parseDocx } = await import('./importer.js?v=20260701a');
    parsed = await parseDocx(file);
  } catch (e) {
    showToast(e.message || '文件解析失败，请确认是工具生成的报告');
    return;
  }

  // 获取所有草稿
  const drafts = await listDrafts();

  // 显示合并选择面板
  const { showMergePanel } = await import('./ui.js?v=20260701a');

  showMergePanel({
    parsed,
    drafts,
    onConfirm: async (targetDraftId, reportType) => {
      state.reportType = reportType;

      if (targetDraftId) {
        // 合并到已有草稿
        const existing = await getDraft(targetDraftId);
        if (existing) {
          state.items = [...(existing.items || []), ...parsed.items];
          state.headerInfo = existing.headerInfo || {
            company: FIXED_COMPANY,
            department: FIXED_DEPARTMENT,
            date: getTodayStr(),
            inspectionDate: getTodayStr(),
            halfMonth: reportType === '5s' ? 'first' : null,
          };
          state.currentDraftId = targetDraftId;
        }
      } else {
        // 创建新草稿
        state.items = parsed.items;
        state.headerInfo = {
          company: FIXED_COMPANY,
          department: FIXED_DEPARTMENT,
          date: getTodayStr(),
          inspectionDate: getTodayStr(),
          halfMonth: reportType === '5s' ? 'first' : null,
        };
        state.currentDraftId = null;
      }

      // 保存
      if (state.items.length > 0) {
        saveDraft(state.reportType, {
          items: state.items,
          headerInfo: state.headerInfo,
        }).then(newId => {
          state.currentDraftId = newId;
        }).catch(e => console.error('保存草稿失败:', e));
      }

      showToast(`已导入 ${parsed.items.length} 条`);
      showItemList();
    },
    onCancel: () => {
      // 用户取消，回到首页
    },
  });
}

// ---------- 导入照片 ----------

async function handleImportPhoto(file) {
  // Task 11 实现照片 OCR
  showToast('照片导入功能即将上线');
}
```

- [ ] **Step 5: 确认 import 语句更新**

确保 `js/app.js` 顶部 import 了所需函数：

```js
import { saveDraft, getDraft, deleteDraft, listDrafts, getPresets, savePresets, getTodayStr, migrateFromV1 } from './db.js?v=20260701a';
```

- [ ] **Step 6: 验证语法**

Run: `node --check js/ui.js && node --check js/app.js`
Expected: 无输出

- [ ] **Step 7: Commit**

```bash
git add js/ui.js js/app.js
git commit -m "feat: 导入入口+合并面板 — 首页导入按钮, 解析后选择合并目标"
```

---

### Task 10: 集成测试 — 草稿系统 + AI 优化

**文件:**
- 无新建，测试现有功能

**功能:** 用浏览器测试三项改进的前两项。

- [ ] **Step 1: 本地启动测试服务器**

Run: `npx serve . -p 3000`
打开浏览器访问 `http://localhost:3000`

- [ ] **Step 2: 测试草稿系统**

1. 新建一个安全自查报告，添加 2 条问题 → 返回首页
2. 确认草稿箱显示 `(1/6)`，有类型标签和删除按钮
3. 再创建 5S 和公司报告各一个
4. 确认草稿箱数量增加
5. 点击某条草稿的 🗑️ 按钮 → confirm 确认 → 草稿消失
6. 点击草稿卡片 → 继续编辑

- [ ] **Step 3: 测试 AI 优化取消和跳过**

1. 进入问题编辑页，填写描述，点击 ✨ AI润色
2. 在等待动画中，点击 ✕ 取消 → 返回编辑页（不报错）
3. 再次 AI 润色，等待结果
4. 结果页出现 3 个选项 + 底部「📋 直接使用原文」按钮
5. 点击「直接使用原文」→ 返回编辑页，描述为原文

- [ ] **Step 4: 测试 AbortController 真正中断请求**

1. 打开浏览器 DevTools → Network 标签
2. 触发 AI 润色
3. 在 Network 中看到 fetch 请求
4. 点击取消按钮
5. 确认 Network 中该请求状态为 `canceled`

- [ ] **Step 5: Commit（如有修复）**

如有修复，提交：
```bash
git add -A
git commit -m "fix: 集成测试修复"
```

---

### Task 11: importer.js — 照片 OCR（豆包视觉模型）

**文件:**
- 修改: `js/importer.js` — 增加 `parsePhoto` 函数
- 修改: `js/app.js` — `handleImportPhoto` 实现

**前置条件:** 豆包视觉模型 API 可用（需验证）

- [ ] **Step 1: 验证豆包视觉模型 API**

在 `js/importer.js` 末尾增加 OCR 函数前，先用 curl 测试 API 可用性。

豆包视觉模型使用 chat/completions 接口（和文本润色是同一个 API），只是 model 不同。视觉模型 endpoint ID 需要确认。

如果视觉模型不可用，跳过此 Task，`handleImportPhoto` 退化为简单图片导入（照片仅作为图片，描述留空）。

- [ ] **Step 2: 在 importer.js 中增加 parsePhoto 函数**

```js
/**
 * 用豆包视觉模型从照片中提取问题描述
 * @param {File|string} photo — File 对象或 base64 data URL
 * @returns {Promise<{description: string, photo: string}>}
 */
async function parsePhoto(photo) {
  // 1. 转为 base64
  let dataUrl;
  if (photo instanceof File) {
    dataUrl = await new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.onload = () => resolve(reader.result);
      reader.onerror = reject;
      reader.readAsDataURL(photo);
    });
  } else {
    dataUrl = photo;
  }

  // 2. 压缩图片
  const compressed = await compressImageForOCR(dataUrl);

  // 3. 调用豆包视觉模型（使用 chat/completions + vision model）
  // 注意：视觉模型 endpoint ID 需确认
  const VISION_MODEL = 'ep-20260616232549-wr6bn'; // 先用文本模型测试，如果不支持视觉再换
  const API_URL = 'https://ark.cn-beijing.volces.com/api/v3/chat/completions';
  const API_KEY = 'ark-4b152d9d-0ad1-4e65-838f-a52f264ff4ea-12064';

  // 去掉 data:image/...;base64, 前缀
  const base64Data = compressed.split(',')[1] || compressed;

  const response = await fetch(API_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${API_KEY}`
    },
    body: JSON.stringify({
      model: VISION_MODEL,
      messages: [
        {
          role: 'user',
          content: [
            {
              type: 'image_url',
              image_url: { url: `data:image/jpeg;base64,${base64Data}` }
            },
            {
              type: 'text',
              text: '请识别这张安全检查照片中的问题，用简洁的整改报告书面语言描述。只输出一句话的问题描述，不要加序号、标签或解释。'
            }
          ]
        }
      ],
      temperature: 0.3,
      max_tokens: 300
    })
  });

  if (!response.ok) {
    const errText = await response.text().catch(() => '');
    console.error('[OCR] API 错误:', response.status, errText);
    throw new Error(`AI 识别失败(${response.status})，请手动填写描述`);
  }

  const result = await response.json();
  const content = result.choices?.[0]?.message?.content || '';
  const description = content.trim();

  return {
    description: description || '（AI 未能识别，请手动描述）',
    photo: dataUrl,
  };
}

/**
 * 压缩图片用于 OCR（比修图压缩轻，保留更多细节）
 */
function compressImageForOCR(dataUrl, maxKB = 800) {
  return new Promise((resolve) => {
    const img = new Image();
    img.onload = () => {
      const canvas = document.createElement('canvas');
      let w = img.width, h = img.height;
      const MAX_PX = 1500;
      if (w > MAX_PX || h > MAX_PX) {
        const ratio = Math.min(MAX_PX / w, MAX_PX / h);
        w = Math.round(w * ratio);
        h = Math.round(h * ratio);
      }
      canvas.width = w;
      canvas.height = h;
      const ctx = canvas.getContext('2d');
      ctx.drawImage(img, 0, 0, w, h);
      let quality = 0.9;
      let result = canvas.toDataURL('image/jpeg', quality);
      while (result.length > maxKB * 1024 && quality > 0.3) {
        quality -= 0.1;
        result = canvas.toDataURL('image/jpeg', quality);
      }
      resolve(result);
    };
    img.src = dataUrl;
  });
}

export { parseDocx, parsePhoto };
```

- [ ] **Step 3: 在 app.js 中实现 handleImportPhoto**

替换 Task 9 Step 4 中的 `handleImportPhoto` 占位函数：

```js
async function handleImportPhoto(file) {
  showToast('正在识别照片...');

  try {
    const { parsePhoto } = await import('./importer.js?v=20260701a');
    const result = await parsePhoto(file);

    // 创建单条目的 items 数组
    const items = [{
      description: result.description,
      beforePhoto: result.photo,
      afterPhoto: '',
      status: '待整改',
    }];

    // 获取草稿列表
    const drafts = await listDrafts();
    const { showMergePanel } = await import('./ui.js?v=20260701a');

    showMergePanel({
      parsed: { items, reportType: null },
      drafts,
      onConfirm: async (targetDraftId, reportType) => {
        const rt = reportType || 'safety';
        state.reportType = rt;

        if (targetDraftId) {
          const existing = await getDraft(targetDraftId);
          if (existing) {
            state.items = [...(existing.items || []), ...items];
            state.headerInfo = existing.headerInfo || {
              company: FIXED_COMPANY, department: FIXED_DEPARTMENT,
              date: getTodayStr(), inspectionDate: getTodayStr(),
              halfMonth: rt === '5s' ? 'first' : null,
            };
            state.currentDraftId = targetDraftId;
          }
        } else {
          state.items = items;
          state.headerInfo = {
            company: FIXED_COMPANY, department: FIXED_DEPARTMENT,
            date: getTodayStr(), inspectionDate: getTodayStr(),
            halfMonth: rt === '5s' ? 'first' : null,
          };
          state.currentDraftId = null;
        }

        if (state.items.length > 0) {
          saveDraft(state.reportType, {
            items: state.items,
            headerInfo: state.headerInfo,
          }).then(newId => { state.currentDraftId = newId; })
            .catch(e => console.error('保存草稿失败:', e));
        }

        showToast(`已导入照片，AI 识别描述：${result.description.substring(0, 30)}...`);
        showItemList();
      },
      onCancel: () => {},
    });
  } catch (e) {
    showToast(e.message || '照片识别失败');
  }
}
```

- [ ] **Step 4: 如果视觉模型不可用，降级处理**

如果豆包文本模型不支持 vision（返回 400），在 `parsePhoto` 的 catch 中降级：

```js
  } catch (e) {
    // 视觉模型不可用时降级：照片作为图片导入，描述留空
    if (e.message && e.message.includes('400')) {
      console.warn('[OCR] 视觉模型不可用，降级为纯图片导入');
      return {
        description: '（请手动填写问题描述）',
        photo: dataUrl,
      };
    }
    throw e;
  }
```

- [ ] **Step 5: 验证语法**

Run: `node --check js/importer.js && node --check js/app.js`
Expected: 无输出

- [ ] **Step 6: Commit**

```bash
git add js/importer.js js/app.js
git commit -m "feat: 照片OCR — 豆包视觉模型识别问题描述"
```

---

### Task 12: 收尾 — 版本号更新 + 清理

**文件:**
- 修改: `index.html` — 版本号升级
- 修改: `js/app.js` — import 版本号升级

- [ ] **Step 1: 升级所有版本号**

将 `?v=20260701a` 全部替换为 `?v=20260701b`。

涉及文件：
- `index.html`: `<link href="css/style.css?v=20260701b">` 和 `<script src="js/app.js?v=20260701b">`
- `js/app.js`: 所有 import 的版本号
- `js/ui.js`: 所有 import 的版本号

- [ ] **Step 2: 删除 JSZip 测试代码**

在 `index.html` 中删除 Task 7 Step 3 加的 JSZip 测试 `<script>` 块。

- [ ] **Step 3: 最终测试**

本地启动服务器，完整走一遍流程：
1. 新建报告 → 添加条目 → AI 润色 → 取消 → 再润色 → 使用原文
2. 返回首页 → 草稿箱有记录 → 删除一条
3. 导入 .docx 文件 → 合并到已有草稿
4. 生成报告 → 下载

- [ ] **Step 4: 推送到 GitHub**

```bash
git add -A
git commit -m "chore: 版本号升级至20260701b，清理测试代码"
git push origin master
```

- [ ] **Step 5: 验证线上版本**

等待 GitHub Pages 部署（约 1-2 分钟），访问 `https://fyr0309.github.io/inspection-tool/`。
硬刷新（Ctrl+Shift+R 或 Safari 清除缓存），确认线上版本正常。

---

## 实施总结

| Phase | Task | Commit 消息 | 文件 |
|-------|------|------------|------|
| 1.草稿 | 1 | feat: 草稿系统升级 — keyPath改为id, 支持6条草稿, v1→v2迁移 | db.js |
| 1.草稿 | 2 | feat: 草稿卡片改造 — 类型标签+删除按钮+容量指示 | ui.js |
| 1.草稿 | 3 | feat: 适配新草稿数据结构 — draftId追踪, v1迁移, 保存返回新ID | app.js |
| 2.AI | 4 | feat: AI优化支持取消 — callDoubaoOptimize增加signal参数 | ai.js |
| 2.AI | 5 | feat: AI优化页增加取消按钮+使用原文按钮 | ui.js |
| 2.AI | 6 | feat: AI优化重构 — AbortController取消 + 使用原文跳过 | app.js |
| 3.导入 | 7 | chore: 本地化 JSZip 3.10.1，用于 .docx 导入解析 | lib/, index.html |
| 3.导入 | 8 | feat: 新建importer.js — .docx文件解析, 提取描述和图片 | importer.js |
| 3.导入 | 9 | feat: 导入入口+合并面板 — 首页导入按钮, 解析后选择合并目标 | ui.js, app.js |
| 3.导入 | 10 | 集成测试 | 无 |
| 4.OCR | 11 | feat: 照片OCR — 豆包视觉模型识别问题描述 | importer.js, app.js |
| 收尾 | 12 | chore: 版本号升级，清理测试代码 | 各文件 |
